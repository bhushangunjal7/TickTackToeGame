package org.example.strategies;

import org.example.models.Board;
import org.example.models.Move;

public class HardBotPlayingStatergy implements  BotPlayingStrategy{
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
